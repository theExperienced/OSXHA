import styled from 'styled-components';

const StyledTenant = styled.div`
        background-color: rgba(255, 255, 255, .2);
        padding: 1rem;
        border-radius: 7px;
        
        & button {
            margin-right: 1rem;
            border-radius: 6px;
            background-color: rgba(255, 255, 255, .2);
            padding: .5rem .9rem;
            font-size: 1rem;
            text-transform: uppercase;
            margin-bottom: 1rem;
        }
`;

export default StyledTenant;